import React from 'react'

export default function InfoDrawer() {
  return (
    <Drawer
      width={640}
      placement="right"
      closable={false}
      onClose={onClose}
      visible={visible}
    >
      sfsfsf
    </Drawer>
  )
}
